# RAG_Blog
我做了一个基于RAG的自动化内容生成系统，主要解决大模型生成内容不准确的问题。 整体流程是先对用户query进行rewrite，然后通过embedding做向量检索，再用rerank提升结果相关性，最后基于这些上下文用大模型生成结构化博客内容。 在实现上，我重点优化了检索链路，因为我发现RAG效果很依赖上下文质量，同时我也做了模型抽象、自动发布和评估模块，让系统形成一个完整闭环
